import React from 'react';
import { mount, shallow } from 'enzyme';
import TodoItem from '../TodoItem';

test('Check Todos', () => {
    /* const wrapper = mount(<TodoItem 
        key="1"
        todo="Äta bananer"
        markComplete="1"
        delTodo=""/>);

    expect(wrapper.props().key).toEqual('1');
    expect(wrapper.props().todo).toEqual('Äta bananer');
    expect(wrapper.props().markComplete).toBe('1');
    expect(wrapper.props().delTodo).toBe(''); */

});

/* test('Toggle complete', () => {
    const wrapper = shallow(<TodoItem />);

    wrapper.setProps({ completed: true });
    const checkbox = wrapper.find({ type: 'checkbox' });
    expect(checkbox.props().completed).to.equal(true);

}); */

/* test('Checks JSX', () => {
    const wrapper = mount(<TodoItem />);

    

}); */